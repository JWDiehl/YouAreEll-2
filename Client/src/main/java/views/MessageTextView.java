package views;

import models.Message;

public class MessageTextView {
    //class is to provide a textual view of Message objects

    public String toString(Message m) {
        //toString method - takes a Message object as a parameter and returns a string
        return m.toString();
    } 
}